//import P from 'prop-types';
import { MenuNav } from '../MenuNav';
import * as Styled from './styles';

export const Menu = () => {
  return (
    <Styled.Container>
      <MenuNav />
    </Styled.Container>
  );
};
